import React from 'react';
import { getOtherPage } from '../services/service';
import Personajes from './Personajes';



const Pagination = (props) => {
  const { paginfo, setPersonajes, setInfo } = props;

  return (
    <div>

    </div>
  );
};

export default Pagination;